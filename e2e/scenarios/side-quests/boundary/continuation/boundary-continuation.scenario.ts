import { boundaryScenario } from "../boundary.ts";
export default boundaryScenario({ id: "continuation", heading: "FROM PARENT", sentinel: "FROM_PARENT_EXPANDED_END", ready: "CONTINUATION_BOUNDARY_AFTER", grouping: false });
