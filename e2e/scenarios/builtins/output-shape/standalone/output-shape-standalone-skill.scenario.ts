import { outputShapeScenario } from "../output-shape.ts";

export default outputShapeScenario({
  name: "standalone skill Read preserves output-owned line shape",
  grouped: false,
  skill: true,
});
